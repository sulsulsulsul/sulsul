'use client';
import PracticeList from '@/entities/practice-list';
import { getArchiveList } from '@/entities/practice-list/actions/getList';

const Page = () => {
  return <PracticeList />;
};

export default Page;
